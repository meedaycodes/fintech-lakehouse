# Python virtual environment
finenv/
venv/
.venv/
__pycache__/
*.pyc

# Local Unity Catalog secrets - contains real OAuth client-id/secret and is
# (re)generated per-boot. Copy server.properties.example to get started.
docker/etc/conf/server.properties
docker/etc/conf/token.txt

# Generated data (regenerate via data_gen/generate_data.py)
data/raw/*
!data/raw/.gitkeep

# Delta table files (regenerate via bronze_ingest.py / silver_transform.py)
data/lakehouse/

# Rows that failed validation in silver_transform.py
data/quarantine/

# Spark's own scratch dir - explicitly pointed here by spark_session.py so
# it can't scatter spark-warehouse/ elsewhere depending on cwd.
data/spark-warehouse/

# Terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*

# Jupyter
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
.vscode/
.idea/

# Test / coverage artifacts
.pytest_cache/
.coverage
